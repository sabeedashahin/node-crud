<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/bst/css/bootstrap.min.css" />
  <title>Edit Contact</title>
</head>
<body>
  <form method="post" action="<%= base_url %>/crud/update/<%= editedData._id %>">
    <div class="container">
      <div class="row">
        <div class="col col-md-4 offset-4">
          <h3 class="text-center text-primary mt-4">Edit Contact</h3>
          <div class="form-group">
            <label>Name</label>
            <input type="text" name="name" value="<%= editedData.name %>" placeholder="Name" class="form-control mt-2" />
          </div>
          <div class="form-group">
            <label>Email</label>
            <input type="email" name="email" value="<%= editedData.email %>" placeholder="Email" class="form-control mt-2" />
          </div>
          <div class="form-group">
            <label>Mobile</label>
            <input type="text" name="mobile" value="<%= editedData.mobile %>" placeholder="Mobile" class="form-control mt-2" />
          </div>
          <input type="submit" value="Update" class="btn btn-primary w-100 mt-2" />
        </div>
      </div>
    </div>
  </form>
</body>
</html>
